{
  "name": "rn-mt",
  "private": true,
  "version": "0.1.0",
  "description": "Manifest-driven multitenancy conversion platform for React Native and Expo apps.",
  "license": "MIT",
  "packageManager": "pnpm@10.25.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean && rimraf coverage dist .turbo",
    "dev:cli": "pnpm --filter @rn-mt/cli dev",
    "examples:verify": "node ./scripts/verify-examples.mjs",
    "lint": "pnpm -r lint",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "pnpm -r typecheck",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/react": "^19.1.0",
    "prettier": "^3.4.2",
    "rimraf": "^6.0.1",
    "tsup": "^8.3.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
